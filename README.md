# pillow-pile
A proxy to enable hierarchical documents in CouchDB

# Development
#### Setup a Virtualenv  
    python -m venv venv
#### Install Dev dependencies  
    pip install tornado
    pip install pyyaml

